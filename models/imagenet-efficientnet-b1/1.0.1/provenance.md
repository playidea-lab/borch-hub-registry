# imagenet-efficientnet-b1 1.0.0

**우리가 학습한 가중치가 아니다.** 남이 학습한 것을 옮겨 왔고, 이 파일이
그 출처다.

- 출처: `timm 1.0.28` 의 `efficientnet_b1`
- 체크포인트: `timm/efficientnet_b1.ra4_e3600_r240_in1k`
- 가중치 라이선스: apache-2.0
- 열쇠 508개 · 31,472,156 바이트
- sha256 `6a906de377b3091857f01bc3bb0a7e38e2c1aeabb5e1d290c77840315002085c`

## 발표된 수 — 우리가 잰 것이 아니다

원저자가 발표한 top-1 을 이 화물은 모른다.

매니페스트의 `metrics` 는 비어 있다. 그 자리는 **우리가 잰 수**를 위한
것이고, 여기 적을 수는 우리 것이 아니다. 비트 동등은 borch 의 명시적
비목표이므로 이 수를 그대로 물려받는다고 말할 수도 없다.

## 옮긴 것이 같은 모델인지

카탈로그(`bimm`) 쪽에 `browser/parity.py` 가 있다. timm 을 실제로 세워
가중치·입력·출력을 받아 오고, 같은 가중치를 같은 입력에 통과시켜 수를
나란히 놓는다 — 열쇠 집합과 출력 둘 다 본다.

## 전처리

짧은 변을 `266` 로 키운 뒤 가운데를
`[3, 240, 240]` 로 자른다(crop_pct 0.9).
보간은 `bicubic` — 원본과 같다.

## 샘플

`sample.in.safetensors` 는 시드 0 로 만든
전처리 완료 텐서이고, `sample.out.safetensors` 는 **원본이 낸 수**다.
허브가 이 화물을 실을 때마다 그 수를 재현하는지 보게 된다.
